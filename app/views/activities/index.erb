<%= flash[:message]  %><br>
<% @activities.each do |activity| %>

  <%= activity.name %><br>
  <% flash[:activity_id] = activity.id %>
  <%= button_to "Add Activity", "/activity_instances/new" %>
  <%= button_to "More Info", "activity_path(activity)", {method: :get} %>

<% end %>

<%= button_to "New Activity", new_activity_path, {method: :get} %>
<%= button_to "Back", user_path(current_user), {method: :get} %>
