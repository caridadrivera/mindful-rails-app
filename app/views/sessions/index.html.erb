<!DOCTYPE html>
        <header>
            <nav>
                <div class="row">    <!-- <img src="img relative path goes here" alt="Health logo" class="logo"> -->
                </div>
            </nav>
            <div class="main-text-box">
                <h1>Happy Tracker<br>Where happiness starts</h1>
                <%= button_to "Sign Up", new_user_path, {method: :get} %>
                <%= button_to "Log In", '/login', {method: :get} %>

<header>
    <nav>
        <div class="row">

        </div>
    </nav>
    <div class="main-text-box">
        <h1>Mindful</h1>
       <%= button_to "Sign Up", new_user_path, {method: :get} %>
       <%= render 'form' %>
    </div>

</header>
