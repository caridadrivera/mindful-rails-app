<%= @day.date %><br>
<% @day.activities.each do |activity|%>
  <%= activity.name %><br>
  <%= button_to "delete", @activity_instances , :method => :delete, params: {:activity_id => activity.id} %>
<% end %>

<%= button_to "Back to Activity History", days_path, {method: :get} %>
  
